# Contributing

## Development

1. Install dependencies.
2. Run `npm run typecheck`.
3. Run `npm run test`.
4. Run `npm run build`.

## Pull Requests

- Keep components progressively enhanced by default.
- Prefer opt-in JavaScript for motion behavior.
- Add or update tests when changing component behavior.
